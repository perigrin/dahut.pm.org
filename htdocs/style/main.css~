body
{ 
  background: #aaa;
  color: #000;
  margin: 0px;
  width: 94%;
  margin-left: auto;
  margin-right: auto;
}

.searchmap {
    float:right; width:300px; height: 350px; float:right; border: 1px solid black
}

a:link
{ 
  background: transparent;
  color: #00f
}

a:visited
{
  background: transparent;
  color: #009
}

a:hover
{
  background: transparent;
  color: #00f
}

div#node_image_box {
       clear: right;	
       width: 300px;
       height: 190px;
       float: right;
       margin-top: 5px;
       text-align: right;
}

img#node_image {
       height: 190px;
}

img {
  border: 1px solid black;
  margin: 0px;
}

#map { 
	width: 300px; 
	height: 190px;
   	border: 1px solid black;
}

.map_list { 
	float: right;
	width: 30%;
	height: 400px;
	overflow: auto;
}
	

/* The banner that runs across the top of all nodes */
#banner
{
  padding: 5px;
  background: url(/images/ogsp_banner.jpg) top no-repeat #000;
  border: 1px solid black;
  border-top: none;
}
#banner h1 a {
   color: #eee; 
   text-decoration:none;
}
/* The area underneath the banner on all nodes */
#content
{
  padding: 1em;
}
div#content {
	width: 100%;
	box-sizing: border-box;
	-moz-box-sizing: border-box;
	background-color: white;
	margin-top: 0px;
	margin-left: auto;
	margin-right: auto;
	border: 1px solid black;
	border-top: none;

}
div#content blockquote { font-size: small; display: inline; margin: 0px }
#maincontent { 
  margin-left: 195px;
}
/* The navigation bar that appears on all nodes */
#navbar
{
  float: left;
  width: 175px;
  padding: 0px 5px 5px 5px;
  border: 1px solid #000;
  background-color: #fff;
  color: #000;
  font-size: 0.8em;
  margin: 10px 2em 10px 10px; 
}

/* A group of items in the navbar */
.navbar_group
{
}

/* A title for that group */
.navbar_group_title
{
  margin: .5em 0em 0em 0em;
  font-weight: bold
}

/* An item in the navbar */
.navbar_item
{
  margin-top: 0em;
  margin-bottom: 0em
}

/* The "Search" label, text field and "Go" button in the navbar */
.search_group
{
}
 
/* The "Search" label itself */
.search_label
{
  font-weight: bold
}
.navbar_site_name {
    font-size:1.2em;
}
#navbar ul {
    margin-top:0px;
}
/* The link on all nodes saying "edit this page" */
div.edit_page_link
{
  font-weight: bold;
  margin-bottom: 1em;
  padding-top: 1em;
  border-top: 1px solid #000
}

/* The Recent Changes listing on the home page */
#recent
{
  float: right; 
  padding: 1em;
  border: 1px solid #000;
  width: 175px;
  margin: 10px;
  font-size: 0.7em
}

/* An item title in the listing */
#recent dt
{
  font-weight: bold
}

/* An item definition in the listing */
#recent dd
{ }

/* The name of a node */
h2.node_name
{
   font-style: italic;
   padding-top: 0.5em;
   margin-bottom: 0.2em;
}
div#title_edit_link, div#title_comment_link {
    display: inline;
    font-size: 0.8em;
     /* padding-left: 0.5em; */
}

/* Listing of categories and locales for a node */
#categories_and_locales
{
  font-size: small
}

ul#categories_and_locales
{
	  margin-top: 0;
	    padding-top: 0;
}
div#metadata ul li {
  list-style: none;
}
div#metadata ul li span.metadata_label {
  font-style: italic;
  margin-left: -1em;
}
/* The widget for distance lookups */
#find_within_distance
{ }

/* The listing of coordinates on nodes with geo metadata */
#coordinates
{ }

/* The node version indicator */
#version
{
  margin-top: 10px;
  font-size: small
}

/* The 'RDF/XML for this node' link that appears on all nodes */
#rdf_link
{
  font-size: small;
  float: right
}

/* The search form that appears on all nodes */
#search_form
{
  display: none;
  padding-top: 10px;
  border-top: 1px solid #999
}

/* The footer that appears on all nodes */
#footer
{
  margin-top: 10px;
  padding-top: 10px;
  border-top: 1px solid #999;
  font-size: 0.8em;
  text-align: right
}

/* Submit buttons in forms */
input.form_button
{
  font-size: 0.9em;
  font-weight: bold;
  background-color: #fff;
  color: #000
}

/* The 'before' part of a diff listing */
.diff1
{
  background-color: yellow;
}

/* The 'after' part of a diff listing */
.diff2
{
  background-color: #C00;
}

/* Hidden elements */
.hidden
{
  display: none;
}

/* Labels for form elements */
.label
{
  font-weight: bold;
}

#topnav {
    display:block;
    width:100%;
    background-color:#eee;
    margin:-5px;
    margin-top: 55px;
    padding: 5px 5px;
}
#topnav ul {
    list-style-type: none;
}
#topnav li {
    display:inline;
    border-right: 1px solid black;
    padding: 5px 0px;
    margin:0px;
}

#topnav li a {
    text-decoration:none; 
    padding: 5px 10px;
    margin:0px;
}

#topnav li a:hover {
    padding: 5px 10px;
    text-decoration:none; 
    background-color:#ccc;
}

#content a[href ^="http://"], #maincontent a[href ^="http://"] { 
    background: url(http://en.wikipedia.org/skins-1.5/monobook/external.png) center right no-repeat;
    padding-right: 13px;
    }
#navbar a[href ^="http://"] { 
    padding-right: 0px;
    background: none;
}
#node_image_box a[href ^="http://"] { padding-right: 0px; background: none; }
#node_image_copyright  a[href ^="http://"] { padding-right: 0px; background: none; } 
#node_image_copyright { font-size: smaller }
/* Catalyst */
body{
    font-family:verdana, arial, sans-serif;
    font-size:10pt;
}
#catListMainTitle{
    font-size:14pt;
    color:#000000;
}
#catListMain{
    list-style: none;
    margin: 0;
    padding: 0;
}
#catListMain li{
    list-style: none;
    display:inline;
    font-size:9pt;
    color:#000000;
    margin: 0;
    padding: 0;
}
#catListMain li:after { content: ","; }
#catListMain li:last-child:after { content: ""; }
#catListAltTitle{
    font-size:14pt;
    color:#000000;
}
#catListAlt{
    margin-top: 0;
    padding-top:0;
    /*padding:0;
    margin:0;*/
}
#catListAlt li{
    list-style-type: square;
    font-size:9pt;
    color:#000000;
    margin-top: 0;
    padding-top:0;
}

#banner #summary { display: none }
.navbar_site_name { display: none }

#rdf_link { display: none } 
#version { float: right; font-size: 8pt; color: #AAA }
#version a { color: #AAF }

/* Edit Pages */
  div.edit_form_actions { text-align: center; }
  div.preview_edit { float: left; width: 33%; }
  div.cancel_edit { float: left; width: 33%; }
  div.commit_edit { float: right; width: 33%; }
  fieldset#main_information { clear: both; }

fieldset legend { font-weight: bold; }
  fieldset { margin-left: 5%; margin-right: 5%; }

fieldset label { display: block; width: 15%; float: left; }
fieldset div.edit_box { width: 80%; float: right; }
fieldset div.edit_form_group { clear: both; }

